# Mock Environment Configuration
# This file contains safe defaults for testing and development

# Mock GraphQL endpoint - no real network calls
DEFAULT_GRAPHQL_ENDPOINT="http://localhost:4000/graphql"

# Redis configuration for local testing
REDIS_URL="redis://localhost:6379"

# Security settings
NODE_ENV="test"

# Optional: Mock API keys (for testing only)
# API_KEY="mock-api-key-for-testing"
